<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.every.admin.professor.position.model.ProfessorPositionDAO">

	<select id="selectPosition" resultType="professorPositionVO">
		select * from prof_position order by position_no
	</select>
	
	
	
</mapper>









